import { ArrowRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import { hotels } from '../data/travel'
import { experiences } from '../data/catalog'

export default function DestinationOverview({ item, day, setDay }) {
  return <section className="detail-content container"><div className="detail-main"><div className="detail-intro"><span className="eyebrow"><i />The feeling of {item.name}</span><h2>A little closer to<br />what you’re imagining.</h2><p>{item.description} The best way to meet this place is slowly—leave room for a conversation, a long lunch, and a route that changes with the weather.</p><blockquote>“Go for the view. Stay for the feeling that you have all the time in the world.”</blockquote></div><div className="detail-image-grid"><img src={item.image} alt={`${item.name} landscape`} /><img src={experiences[0].image} alt={`${item.name} experience`} /><img src={hotels[3].image} alt={`${item.name} stay`} /></div><div className="attractions"><div className="section-mini-head"><div><span className="eyebrow"><i />Worth making time for</span><h2>Don’t miss these</h2></div><Link to="/guide">Full travel guide <ArrowRight size={15} /></Link></div><div>{item.attractions.concat(['Local food walk', 'A quiet viewpoint']).map((place, index) => <article key={place}><span>0{index + 1}</span><div><h3>{place}</h3><p>A memorable place, with a little guidance on when and how to go.</p></div><ArrowRight size={17} /></article>)}</div></div><div className="itinerary-preview"><div className="section-mini-head"><div><span className="eyebrow"><i />A place to begin</span><h2>Three days, well spent</h2></div><span className="pill pill--mint">FlyMate suggested</span></div>{['Arrive and find your rhythm', 'Go where the city wakes up', 'Leave with one more sunset'].map((title, index) => <button className={day === index + 1 ? 'is-active' : ''} onClick={() => setDay(index + 1)} key={title}><span>Day {index + 1}</span><strong>{title}</strong><small>{index === 0 ? 'Easy arrival, a local dinner, and an early night.' : index === 1 ? 'A landmark morning and an unhurried afternoon.' : 'A final view, one last coffee, then home.'}</small></button>)}</div></div></section>
}
